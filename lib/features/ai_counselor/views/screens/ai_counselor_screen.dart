import 'package:flutter/material.dart';

class AiCounselorScreen extends StatelessWidget {
  const AiCounselorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AI Counselor')),
      body: const Center(
        child: Text('AI Counselor Screen - Coming Soon'),
      ),
    );
  }
}